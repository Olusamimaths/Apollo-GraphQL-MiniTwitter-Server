# Apollo-GraphQL-MiniTwitter-Server

## Technologies
  - Node.js
  - Git
  - MongoDb
  - Express
  - Graphql
  - Apollo Server

## Setup 
clone the repo and install dependecies `npm install`,
visit `http://localhost:5000/graphql` to test the Graphql api
